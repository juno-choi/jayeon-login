spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: login-service
#      profile:
management:
  endpoints:
    web:
      exposure:
        include: health, httptrace, busrefresh