spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: login
#      profile:
management:
  endpoints:
    web:
      exposure:
        include: refresh, health, httptrace, busrefresh